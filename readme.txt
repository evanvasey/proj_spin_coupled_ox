The main code is located inside the folder "bond_breaking_class". It uses a class mc_state to represent multiconfigurational states.
The folder "gene_csf" is a work in progress of a code that generates CSFs using the genealogical coupling method.
The folder "tests" contains different files that might be of use for some test and the previous version of the code that was not using a class mc_state
